import { Stack } from "expo-router";

const experienceLayout = () => {
  return (
    <Stack
      screenOptions={{
        headerShown: false,
      }}
    />
  );
};

export default experienceLayout;
